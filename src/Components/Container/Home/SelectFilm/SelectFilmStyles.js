import { css } from 'glamor';

const selectFilmStyles = () => css({
    background: 'red',
    margin: '20px 20px',
    height: '100%',
    ' img': {
        height: 200,
        padding: '20px 6px',
    },
});

export default selectFilmStyles;

export const stylesMaterial = {
    iconWhite: {
        color: '#fff'
    }
};
